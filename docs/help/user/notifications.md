# Notifications

Same as [Alerts](release_alerts) — open **Alerts** in the nav.

Unread rows are brighter; dimmed rows are already read. Use **Mark read** / **Mark unread** on each alert, or **Mark all read** when any are unread.
