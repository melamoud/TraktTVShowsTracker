# Latest shows

Same idea as [Latest movies](latest_movies): Trakt `/shows/updates` — recently added/changed **in Trakt’s database**, not airdate calendars. Same lazy sync and defaults (**recent years** + **Matches only**).

Defaults to **Matches only** (purple genres/keywords). Use **Show all** for the full noisy feed.
