# Admin overview

Admins are normal Trakt users with extra local powers:

- Manage users (disable, revoke sessions, delete local data, promote/demote)
- Manage default streaming services and approve suggestions
- Use the app personally with their own Trakt account

Admin bootstrap is controlled by `ADMIN_TRAKT_USERNAMES` in `.env` (see System configuration).
