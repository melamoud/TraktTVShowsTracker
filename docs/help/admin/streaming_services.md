# Streaming services

Maintain the shared default list users pick from. When a user adds a custom service and checks “suggest to admin”, it appears under **Pending suggestions** for approve/reject. Approving adds it to defaults and notifies the user.
